version: "3.9"

services:
  flaskapp:
    build: .
    container_name: flask-baby
    ports:
      - "5000:5000"
    environment:
      ASAAS_TOKEN: "${ASAAS_TOKEN}"
    volumes:
      - .:/app
    restart: always